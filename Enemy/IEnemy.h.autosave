#ifndef ENEMY_H
#define ENEMY_H

#include <Game/Game.h>

class IEnemy
{
public:

    enum types {
        solider,
        bigSolider,
        monsterSolider,
        smartSolider,
        fastSolider
    } type;

    IEnemy(enum types typeOfCannon,
          class Game* Window);

    QPoint center;
    QPoint globalCenter;

    Game* window;
};

#endif // ENEMY_H
